plugins {
    id 'java'
}

repositories {   //
    mavenLocal() // in order to get the dependency from maven local repository
}                //

dependencies {
    implementation 'ru.clevertec:utils:1.3.5'  // in order to get the dependency from maven local repository

//    implementation fileTree(dir: 'libs', include: ['*.jar']) // in order to get the dependency from libs directory
}